# Oracle Systems — Crypto (MVP)
Goal: scan a small symbol list from Binance, compute simple signals (movers / SMA cross),
expose them via a tiny API, and print them in the console. Start local; DB/UI later.
